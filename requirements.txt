yt-dlp>=2025.8.11
Flask>=3.0
psutil>=5.9.0
mutagen>=1.47.0
moviepy>=1.0.3
imageio-ffmpeg>=0.4.9

# Notes:
# - ffprobe/ffmpeg is NOT required at system level for basic operation.
#   moviepy uses imageio-ffmpeg which bundles a portable ffmpeg binary.
# - When system ffprobe is available, it can be used as a fallback.